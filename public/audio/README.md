# Audio

Optional ambient/engine audio. The HUD has a mute toggle wired to the store
(`muted`). To add sound, load clips here and gate playback on `useGameStore`.
Suggested: low engine hum, UI blips, ambient synth pad (all CC0).
