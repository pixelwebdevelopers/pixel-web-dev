# Textures

Optional. The world uses procedural materials + a runtime-generated canvas
texture for road light-trails (see components/world/Roads.tsx). Add compressed
.ktx2/.webp textures here if you extend the environment.
